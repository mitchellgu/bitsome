<section class="module panel <%= expandable ? 'expandable' : '' %> columns large-4 medium-6 small-12">
  <h5 class="panel-heading"><%= title %></h5>
  <div data-load="<%= content_path %>" id="<%= name %>_module" data-load-order="<%= load_order %>">
    <div>
		  <div class="dashboard-module-spinner-container">
		    <i class="fa fa-circle-o-notch fa-spin fa-2x"></i>
		  </div>
		</div>
  </div>
</section>